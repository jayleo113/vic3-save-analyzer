# 项目维护指示

本文件是用户确认采用的完整优化建议，适用于本仓库所有开发、重构、测试与交付工作。
用户后续明确要求优先于本文件。本文描述目标与约束，不等于所有条目已经实现或验收。

## 一、项目目标与优先级

Victoria 3 存档读取器的核心目标是：一次读取、长期复用、数据准确。

按以下顺序处理冲突：

1. 来源和数据正确，不能把旧结果当成最新结果。
2. 保留数据覆盖范围和细节，不通过减少人口、国家、战争或其他内容提速。
3. 减少首次读取耗时，减少重复扫描与重复计算。
4. 模块职责清晰，维护方便；只为明确的维护或性能收益重构。
5. 简洁、可靠的终端体验和便于分享的 Markdown 报告。

本项目主要导出客观数据和体系化国家档案。推演、玩法建议和长篇解释由其他分析框架处理，不应成为基础导出的必要步骤。

## 二、来源、日期与最新存档

- “导出最新”默认选择最近实际保存的文件，选择规则须明确、一致，覆盖手动存档和自动存档。
- 扫描候选文件时只读取必要的头部或元数据，禁止为了显示列表而解析整个存档。
- 不按一月一日、七月一日等日期筛选；任意游戏内日期都应可识别。
- 国家与日期以存档内部记录为准；文件名只提供辅助线索，无法确认时明确显示未知。
- 保留文件名身份与内部身份，两者不同不自动视为错误，也不能用外部日期覆盖正文日期。
- 报告文件名、正文、索引、缓存清单必须指向同一个实际数据截面。
- 国家向用户显示中文名称。内部可保留原始标签和编号，用于关联、追溯与避免同名混淆。
- 报告记录来源文件、内部身份、数据版本和源文件完整校验值。
- 同路径被覆盖、同名同日期但内容不同、游戏在读取期间再次保存，都必须正确处理。

## 三、缓存正确性与失败保护

- 文件大小、修改时间、首尾抽样哈希只用于快速筛选，不能作为内容完全相同的证明。
- 正式复用以完整 SHA-256 或同等完整内容校验为依据。尽量把校验合并到必要的读取流程，避免多次无意义读盘。
- 转换后的文本、基础数据和最终报告都要有明确的校验及版本依据。
- 读取前后确认源文件稳定；变化时拒绝发布混合或过期结果，给出可重试的提示。
- 新结果先写到临时位置，全部完成后发布；失败不能破坏上一份成功结果。
- 并发任务使用进程间锁或数据库事务，防止索引更新丢失和临时文件互相覆盖。
- 数据库连接和文件句柄必须明确关闭，尤其注意 Windows 文件占用导致的移动、删除失败。
- 缓存缺失、部分输出丢失、内容校验失败或版本不兼容时重新生成必要部分。
- 中断后允许重试；清理仅针对明确由当前任务生成的临时文件，不擅自删除原始存档或历史数据。

## 四、同一存档的一次读取与多种导出

目标流程：发现存档 → 确认来源 → 必要的格式转换 → 建立数据块索引 → 提取基础模块 → 保存快照 → 按需筛选 → 生成报告。

- 基础数据以整个存档为单位，不因导出 5 国、30 国或单国而分别重新扫描。
- 将国家、地区、建筑、人口、制度、市场、外交、战争等划分为有明确输入输出的模块。
- 已完成的模块可独立复用；精简人口模式升级到完整人口时，只补充缺少的模块及其必要依赖。
- 报告层从基础数据派生视图，重新计算当前范围相关的比例，不能直接冒用另一范围的结果。
- 导出范围变化时保持关联完整：条约与条款、战争与参战方、战斗与伤亡、宗主国与附属国不能被错误拆散。
- 明确哪些指标是全球分母，哪些是所选国家分母，筛选后不得混用。

## 五、不同年份的复用与存储

- 同一国家不同日期保留独立快照；不能因为国家名相同就沿用人口、经济、疆域或外交旧值。
- 中文名称、商品与建筑定义等静态资料可按游戏版本、模组和本地化环境共享。
- 未变化的数据块或模块，只有在所有实际依赖的内容与解析版本均一致时，才能跨快照复用。
- 建立依赖指纹，包含相关数据块、国家或地区映射及必要的文化、本地化、版本信息。
- 使用内容寻址存储去重；重复模块不必物理保存多份，同一快照可以引用共享对象。
- 解析逻辑与报告展示逻辑分开版本化。仅修改菜单或报告措辞，不应强制重读全部基础数据。
- 不同战役、游戏版本或模组之间不能仅按国家编号或标签自动认定历史连续。
- 跨年份提速幅度取决于实际未变化的数据。用命中模块数与实测耗时描述，不承诺未经验证的复用比例。

## 六、读取性能与最终技术栈

- Python 保留为主程序，承担终端交互、任务编排、报告和模块组织。
- SQLite 承担可查询的持久化数据、关联、索引和快照记录。
- 社区 Garibaldi/Rakaly 等现有工具负责其支持的存档解码，保留来源、许可证和可验证的回退路径。
- JSONL 作为兼容交换方式按需提供，避免每次默认把同一批数据保存为多份镜像。
- Rust 是经性能测量后可采用的局部加速选项，不是必须全量重写的目标。只有确认瓶颈并证明收益后才引入。
- 优先排查反复遍历全文、循环中复制剩余大字符串、重复解码、重复关联和一次性载入无关大表。
- 建立数据块位置索引，解析模块按位置定位；条目扫描使用偏移，避免不断复制后缀。
- 为国家、地区、建筑、人口、市场和战争的常用关联建立合适索引。
- 分阶段记录读取、解码、提取、缓存、筛选、报告和写盘耗时，以实测决定优化方向。
- 测试区分首次解码、首次提取、同视图缓存、换范围导出、补充人口与不同年份；不能把缓存命中耗时当作首次读取性能。
- 不为了拆模块而拆模块；每次调整都应说明改善了哪个职责边界、重复计算或维护问题。

## 七、历史与资料完整性

基础资料至少覆盖：国家概况、GDP及占比、历史趋势、建筑、公司、人口结构、文化、宗教、法律、利益集团、政治运动、市场、条约、国际关系、宗主国与附属国、战争、历史战争、军队、战斗、伤亡和地区变化。

- 区分存档直接记载的历史、两个快照之间的差异，以及无法确定的过程。
- 有战争、战斗、和平或地区转移记录时保留日期、参与方与原始关联。
- 快照比较可以显示疆域、国家记录、附属关系及制度变化，但不能凭变化自动判定战争原因或胜负。
- 报告缺少某国、表格未扫描或字段缺失，不能当作灭亡、零人口或零伤亡。
- 对单个存档已丢弃的历史明确说明不可恢复，不制造“历史上所有战争均已还原”的结论。
- 跨存档历史对照由明确的存档选择或可靠战役标识建立，不自动混合不同游戏局。

## 八、终端体验与输出位置

主入口收敛为：

1. 导出最新 MD。
2. 选择存档批量导出。
3. 资料库与设置。

- 候选存档显示中文国家、游戏日期和必要的来源信息，让用户一次多选。
- 进度在同一行更新，不刷满终端，不使用井号条和无意义的流程说明。
- 可计数时显示真实完成量，例如“读取人口 · 21,000 / 42,076条”。
- 无法可靠计算总量时仅显示当前状态；不模拟进度，不显示不可信的预计剩余时间。
- 终端宽度按中文字符实际显示宽度处理，窄窗口不能因换行产生重复进度。
- 批量完成后显示成功数、失败数、实际总耗时、输出位置，并指出失败项。
- 导出成功后打开文件夹，不自动打开 MD 文件。
- 默认 MD 输出到桌面的 `Victoria3存档MD报告` 第一级目录，直接放中文国家与游戏日期命名的报告，不再为每份报告套子目录。
- 桌面只放用户需要的 Markdown 报告及必要的 Markdown 索引。解析缓存、数据库、日志、测试和转换中间文件留在 F 盘项目内。
- 完整表格导出、历史对照、AI/API 等能力放在次级菜单。基础 MD 导出不依赖外部 API。

本机项目位置：`F:\vic3-save-analyzer`。核心代码应基于项目路径定位存储，便于项目迁移，避免把本机用户名写死进通用逻辑。

## 九、验收标准

必须用多个真实存档验证，并保留可复查的结果：

1. 不同大小、不同年份，以及可获得的文本、压缩或二进制格式。
2. 首次读取、重复导出、改名复制、同路径覆盖与手动任意日期存档。
3. 同存档不同国家范围、精简人口升级完整人口、相邻年份。
4. 中途失败重试、缓存损坏、并发索引更新与源文件在读取时变化。
5. 报告文件名、正文日期、源文件身份的一致性。
6. 新旧版本的表格行数、稳定排序、关键指标和内容校验值一致性。
7. 国家、条约、战争、伤亡和附属关系等跨表关联正确。
8. 桌面 MD、批量导出、HTTP/数据库读取与终端窄窗口的实际流程。

发现结果变化时先查原因。合理修正可以接受，但必须说明变化原因和影响，不能简单把差异视为通过。
性能结果同时列出模式、人口范围、缓存状态和环境条件，不使用不同配置的数据宣称同条件提速。

## 十、执行顺序与发布

按“准确性与阶段计时 → 单次扫描与共享基础库 → 跨年份依赖复用 → 必要的局部加速 → 终端收尾 → 综合验收”推进。

- 完整采纳上述目标，但逐项用实现和测试证据确认完成，不把方案文档当作完成证明。
- 长时间间隔后先读本文件、当前代码差异及已有测试结果，接续工作，不默认从头重构。
- 保留用户和其他任务的既有改动，不擅自回退。
- 当前 v0.3 工作保留本地。用户已要求测试后再决定发布，未经其后续明确确认，不上传 GitHub、不打发布标签。
- 交付时说明实际改动、测试结果、估计使用时间及仍存在的限制。测试结果与进展记录保存在项目内。

本文件于 2026-09-05 根据用户“完整采纳建议，并作为项目指示文件”的要求建立。
